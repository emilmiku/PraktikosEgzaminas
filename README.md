To run my SQL using docker use this in terminal:
docker run --name exam-mysql -e MYSQL_ALLOW_EMPTY_PASSWORD=yes -p 3306:3306 -d mysql:8.4.9

Klonuoti repozitorija  terminale:
1. Terminale nueiti į vietą, kur noresiu pullint. cd kur nueiti.
2. set up ssh key (is github)
3. git clone

parsipulinti savo paruoštuką, ir tada ant jo galiu dirbti.
Komandos: git add .
git commit -m "message"
git push